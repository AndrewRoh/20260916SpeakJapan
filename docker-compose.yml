services:
  app:
    build: .
    container_name: speak-nihongo
    restart: unless-stopped
    ports:
      - "30020:8787"
    env_file:
      - server/.env
    volumes:
      - ./server/gcp-service-account.json:/app/server/gcp-service-account.json:ro
