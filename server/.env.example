# Gemini (레슨 생성용)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Google Cloud Text-to-Speech
# ADC(Application Default Credentials) 인증에 쓰이는 서비스 계정 JSON 파일 경로
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json

# 서버 포트
PORT=8787
